services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: f1_strategy
    ports:
      - "5432:5432"

  api:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
    depends_on:
      - db